#include <stdio.h>
/**
 * main -- entry point
 * prints a given messagge
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
